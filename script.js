const student = {
  name: "John",
  city:"dimapur"
};

// Function to get all keys from the object
function getKeys() {
  console.log(Object.keys(student));
}

// Testing the function
getKeys(); // Output: ["name"]
